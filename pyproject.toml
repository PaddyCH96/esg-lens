[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "esg-lens"
version = "0.1.0"
description = "Transparent, reproducible ESG signal aggregation for public equities"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.115.0",
    "uvicorn==0.30.1",
    "pydantic==2.9.0",
    "pydantic-settings==2.4.0",
    "httpx==0.27.0",
    "tenacity==8.5.0",
    "structlog==24.4.0",
    "pyyaml==6.0.1",
    "yfinance==0.2.40",
    "transformers==4.44.0",
    "torch==2.4.0",
    "spacy==3.7.5",
    "pandas==2.2.2",
    "python-dotenv==1.0.1",
]

[project.optional-dependencies]
dev = [
    "pytest==8.3.0",
    "pytest-asyncio==0.23.5",
    "respx",
    "hishel",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"
