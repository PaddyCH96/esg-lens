---
phase: 01-collectors
plan: "01-03"
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/esg_lens/collectors/edgar.py
  - src/esg_lens/collectors/yfinance_meta.py
  - scripts/backfill.py
  - pyproject.toml
  - tests/unit/test_edgar_split.py
  - tests/unit/test_yfinance_meta.py
  - tests/integration/test_edgar.py
  - tests/integration/test_backfill.py
autonomous: true
requirements: [COLL-04, COLL-05, COLL-07]
must_haves:
  truths:
    - "EDGAR collector resolves ticker to CIK via cached company_tickers.json 7d TTL, pulls submissions for CIK##########.json parallel-array filings, fetches 10-K primary doc, splits to keep Item 1 and Item 1A only via beautifulsoup4, one raw_documents row per section with filing_section set"
    - "yfinance provider runs sync yfinance calls via asyncio.to_thread, seeds company_aliases variants dropping Inc Corp plc etc., writes sustainability score only to companies.external_esg_score never to signals"
    - "scripts/backfill.py --tickers AAPL,XOM populates raw_documents and companies/company_aliases, and re-running adds zero rows via content_hash dedup"
    - "All six Phase 1 Definition of Done criteria are satisfied after this plan completes"
  artifacts:
    - path: "src/esg_lens/collectors/edgar.py"
      provides: "EdgarCollector with CIK cache 7d, submissions, 10-K split Item 1/1A"
      contains: "class EdgarCollector"
    - path: "src/esg_lens/collectors/yfinance_meta.py"
      provides: "YFinance metadata provider with alias variants and external_esg_score isolation"
      contains: "asyncio.to_thread"
    - path: "scripts/backfill.py"
      provides: "CLI orchestrator tickers → yfinance → GDELT → EDGAR with dedup"
      contains: "backfill"
    - path: "pyproject.toml"
      provides: "beautifulsoup4 dependency for 10-K HTML parsing"
      contains: "beautifulsoup4"
    - path: "tests/integration/test_backfill.py"
      provides: "Backfill idempotency test proving re-run adds zero rows"
      contains: "INSERT OR IGNORE"
  key_links:
    - from: "src/esg_lens/collectors/edgar.py"
      to: "src/esg_lens/collectors/http.py"
      via: "GET company_tickers.json, submissions, and filing HTML through shared AsyncHttpClient"
      pattern: "get_http_client"
    - from: "src/esg_lens/collectors/yfinance_meta.py"
      to: "src/esg_lens/collectors/http.py"
      via: "does NOT use http client for yfinance; uses asyncio.to_thread wrapping yfinance Ticker"
      pattern: "asyncio.to_thread"
    - from: "scripts/backfill.py"
      to: "raw_documents"
      via: "inserts via DocumentRepo INSERT OR IGNORE on (ticker, content_hash) for dedup"
      pattern: "INSERT OR IGNORE"
    - from: "scripts/backfill.py"
      to: "companies"
      via: "upserts companies and seeds company_aliases via CompanyRepo"
      pattern: "CompanyRepo|company_aliases"
---

<objective>
Implement EDGAR collector with 7d CIK cache and 10-K Item 1/1A split, yfinance metadata provider with alias seeding and external_esg_score isolation, and the backfill CLI that orchestrates all collectors with content_hash dedup to satisfy the full 6-criteria Definition of Done.

Purpose: EDGAR provides the only G-pillar filings (10-K/8-K/DEF 14A) and must correctly handle SEC's dict-keyed company_tickers.json, parallel-array submissions, and malformed HTML. yfinance is the only free source of sector/market_cap and alias expansion and must not block the event loop or leak sustainability into scoring. Backfill is the single entry point that proves the whole phase: --tickers AAPL,XOM populates raw_documents, re-run adds zero rows, failures never raise, rates and UA are correct, yfinance scores are isolated, and tests never hit live network.
Output: Replaced edgar.py and yfinance_meta.py, new scripts/backfill.py, beautifulsoup4 added to pyproject.toml, and unit plus integration tests proving CIK caching, Item split, yfinance variants, and idempotent backfill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-collectors/01-CONTEXT.md
@.planning/phases/01-collectors/01-RESEARCH.md
@src/esg_lens/collectors/edgar.py
@src/esg_lens/collectors/yfinance_meta.py
@src/esg_lens/collectors/base.py
@src/esg_lens/collectors/http.py
@src/esg_lens/collectors/gdelt.py
@src/esg_lens/db/schema.sql
@src/esg_lens/db/engine.py
@src/esg_lens/db/repositories.py
@src/esg_lens/config.py
@config/sources.yaml
@pyproject.toml
@scripts/init_db.py
@tests/fixtures/edgar_company_tickers.json
@tests/fixtures/edgar_submissions_AAPL.json
@tests/fixtures/edgar_10k_item1_excerpt.html
@tests/fixtures/yfinance_AAPL_info.json
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace EdgarCollector with CIK cache 7d and 10-K Item 1/1A split via beautifulsoup4</name>
  <files>src/esg_lens/collectors/edgar.py, pyproject.toml, tests/unit/test_edgar_split.py</files>
  <read_first>src/esg_lens/collectors/edgar.py, src/esg_lens/collectors/base.py, src/esg_lens/collectors/http.py, src/esg_lens/config.py, config/sources.yaml, pyproject.toml, tests/fixtures/edgar_company_tickers.json, tests/fixtures/edgar_submissions_AAPL.json, tests/fixtures/edgar_10k_item1_excerpt.html, src/esg_lens/db/schema.sql</read_first>
  <action>Replace src/esg_lens/collectors/edgar.py entirely. Implement EdgarCollector extending Collector with source edgar. Implement CIK resolution: async get_cik(ticker, force_refresh) that checks cache file data/cache/company_tickers.json with 7d TTL (86400*7 seconds) loading ticker uppercased to cik_str mapping from SEC https://www.sec.gov/files/company_tickers.json which is dict keyed by integer strings {"0":{"cik_str":320193,"ticker":"AAPL",...}} not a list, handle both dict and list shapes building mapping, fetch via get_http_client with UA containing contact email and headers Accept-Encoding gzip deflate Host www.sec.gov when cache stale or missing, write cache atomically, return cik_str as zero-padded 10-digit string or None; cache read must handle file not found and TTL expiry. Implement submissions fetch: GET https://data.sec.gov/submissions/CIK##########.json with padded CIK (10 digits, path uses CIK##########), headers User-Agent from settings plus Accept-Encoding and Host data.sec.gov, parse filings.recent parallel arrays form accessionNumber filingDate primaryDocument, filter to forms 10-K 8-K DEF 14A keeping recent 5 per form or up to 10 total to bound requests. Implement filing document fetch: for each filing build URL https://www.sec.gov/Archives/edgar/data/{cik_int_no_leading_zeros}/{accession_no_nodash}/{primaryDocument} where accession_no_nodash is accessionNumber with dashes removed and cik_int is cik lstrip 0, GET via get_http_client with same UA, handle 404 fallback and log structlog edgar_filing_not_found then skip. Implement 10-K section split: function split_10k_items(html str) -> dict[str,str] that parses html with BeautifulSoup using lxml if available else html.parser, extracts text via soup.get_text separator newline, then applies regex re.compile(r"^\s*ITEM\s+1A?\b", re.IGNORECASE|re.MULTILINE) to find Item 1 and Item 1A boundaries, handling malformed variants ITEM 1., ITEM 1A - RISK FACTORS, ITEM&nbsp;1A, case-insensitive, keep only Item 1 and Item 1A sections, return {"Item 1": text, "Item 1A": text} or empty dict if neither found (caller logs and skips). For each section create RawDocument with ticker uppercased, source edgar, doc_type filing_section, title f"SEC {form} - {filing_section}", body section text, url filing URL, domain www.sec.gov normalized to sec.gov, external_id accessionNumber, published_at filingDate ISO-8601, filing_type form, filing_section Item 1 or Item 1A, content_hash via base.content_hash, raw_json summary of filing metadata. For 8-K and DEF 14A create single RawDocument per filing without split. Ensure all HTTP goes through get_http_client and never raises; fetch is wrapped by safe_fetch writing collection_runs. Add beautifulsoup4 4.15.0 to pyproject.toml dependencies array alongside existing httpx tenacity structlog etc., and optionally lxml with fallback note. Create tests/unit/test_edgar_split.py testing split_10k_items returns both sections from edgar_10k_item1_excerpt.html, handles HTML with &nbsp; and nested b tags, handles missing Items returning empty dict, handles accession dash removal and CIK lstrip zeros, and that company_tickers dict and list shapes both parse to same mapping; run with PYTHONPATH=src pytest.</action>
  <verify>PYTHONPATH=src pytest tests/unit/test_edgar_split.py -q</verify>
  <acceptance_criteria>
    - pyproject.toml contains beautifulsoup4 dependency string
    - src/esg_lens/collectors/edgar.py contains class EdgarCollector and get_cik and split_10k_items or ITEM\s+1A regex and BeautifulSoup import
    - src/esg_lens/collectors/edgar.py contains company_tickers.json URL and handles dict keyed shape with cik_str and ticker fields and 7d TTL logic
    - src/esg_lens/collectors/edgar.py contains submissions URL with CIK########## padded and parallel arrays filings.recent.form and primaryDocument
    - src/esg_lens/collectors/edgar.py contains accessionNumber replace dash and cik lstrip 0 for Archives URL construction
    - src/esg_lens/collectors/edgar.py contains Item 1 and Item 1A filtering and filing_section assignment and does not keep other items
    - src/esg_lens/collectors/edgar.py uses get_http_client and extends Collector and contains no direct httpx.AsyncClient creation
    - tests/unit/test_edgar_split.py passes with PYTHONPATH=src pytest tests/unit/test_edgar_split.py -q
  </acceptance_criteria>
  <done>EDGAR collector resolves ticker to CIK via 7d cached dict-keyed company_tickers.json, fetches submissions parallel-array filings, fetches primary docs via dash-stripped accession URLs, splits 10-K to Item 1 and Item 1A only via BeautifulSoup plus regex, and one row per section is produced</done>
</task>

<task type="auto">
  <name>Task 2: Replace yfinance provider with asyncio.to_thread and alias variants, create backfill CLI with dedup and verify full Definition of Done</name>
  <files>src/esg_lens/collectors/yfinance_meta.py, scripts/backfill.py, tests/unit/test_yfinance_meta.py, tests/integration/test_edgar.py, tests/integration/test_backfill.py</files>
  <read_first>src/esg_lens/collectors/yfinance_meta.py, src/esg_lens/collectors/base.py, src/esg_lens/collectors/http.py, src/esg_lens/db/repositories.py, src/esg_lens/db/engine.py, src/esg_lens/db/schema.sql, src/esg_lens/config.py, config/sources.yaml, tests/fixtures/yfinance_AAPL_info.json, tests/fixtures/gdelt_sample.json, tests/conftest.py, scripts/init_db.py</read_first>
  <action>Replace src/esg_lens/collectors/yfinance_meta.py with YFinanceMetadataProvider that does not extend Collector HTTP path but provides async fetch_yfinance_metadata(ticker, conn) or similar: implement sync helper _sync_fetch that instantiates yfinance.Ticker(ticker) and reads t.info (property) with try except returning {} on exception, and t.sustainability DataFrame or t.get_sustainability() fallback with try except returning None, then wrap via await asyncio.to_thread(_sync_fetch) with asyncio.wait_for timeout 15 seconds, handle None empty info where shortName missing returns None and logs structlog yfinance_no_info; on success upsert into companies via CompanyRepo using fields name from shortName or longName, sector industry country currency market_cap exchange, cik zero-padded, external_esg_score extracted only from sustainability DataFrame by searching index for totalEsg row and taking iloc 0 0 value if DataFrame not empty, setting external_esg_provider to yfinance only when value present, never feed into esg_signals; alias seeding via helper alias_variants that takes legal name and shortName and generates set of (alias, alias_type) tuples with alias_type legal for legal_name, common for shortName differing, brand for base name with suffixes dropped (list Inc. Inc Corp. Corp Corporation Incorporated plc PLC Ltd. Ltd Limited LLC LP Co.) and also rstrip punctuation variant, insert via INSERT OR IGNORE into company_aliases with columns ticker alias alias_type; ensure graceful None degradation when yfinance returns empty. Create scripts/backfill.py CLI with argparse --tickers comma-separated, --since optional ISO date, --force-refresh flag, --job-id optional; for each ticker uppercased validated against ^[A-Z0-9.-]{1,10}$ and capped at 25, call get_connection, ensure companies row exists via yfinance provider first, then loop collectors GdeltCollector EdgarCollector via safe_fetch with conn ticker since job_id force_refresh, collect list[RawDocument], for each doc compute content_hash and insert via DocumentRepo.insert using INSERT OR IGNORE on (ticker, content_hash) mapping fields ticker source doc_type external_id url domain title body language en published_at filing_type filing_section content_hash collected_at raw_json json dumps if present, track n_fetched and n_new via cursor rowcount; write collection_runs rows already handled by safe_fetch but ensure backfill counts n_new correctly; log structlog with ticker source n_fetched n_new; support idempotency by content_hash UNIQUE. Create tests/unit/test_yfinance_meta.py using unittest.mock.patch patching yfinance.Ticker to return MagicMock with info containing shortName longName sector cik and sustainability DataFrame with totalEsg 25.0, assert that CompanyRepo upsert creates companies row with external_esg_score 25.0 and that alias_variants generates brand variant dropping Inc., and that None sustainability does not create external_esg_score and does not raise. Create tests/integration/test_edgar.py using respx.mock mocking GET to sec.gov/files/company_tickers.json returning edgar_company_tickers fixture dict, GET to data.sec.gov/submissions/CIK0000320193.json returning edgar_submissions fixture, and GET to www.sec.gov/Archives/edgar/data/... returning edgar_10k_item1_excerpt.html, assert EdgarCollector returns 3+ RawDocument rows with filing_section Item 1 and Item 1A and raw_documents domain sec.gov and Uses hishel cache on second call. Create tests/integration/test_backfill.py using respx.mock for GDELT and EDGAR plus patch for yfinance.Ticker to run scripts/backfill logic or import backfill function with in-memory db_conn, assert first run inserts raw_documents rows and companies and company_aliases, second run with same tickers adds zero new rows via content_hash dedup, and that collection_runs rows exist per source with status ok, and that forced collector failure via mocked exception still returns [] and writes collection_runs status failed without raising, and that external_esg_score is isolated to companies table not raw_documents.</action>
  <verify>PYTHONPATH=src pytest tests/unit/test_yfinance_meta.py tests/integration/test_edgar.py tests/integration/test_backfill.py -q</verify>
  <acceptance_criteria>
    - src/esg_lens/collectors/yfinance_meta.py contains asyncio.to_thread or run_in_executor and import yfinance and patch-safe Ticker handling and external_esg_score assignment isolated to companies
    - src/esg_lens/collectors/yfinance_meta.py contains alias_variants or suffix list with Inc. Corp. plc Ltd. and company_aliases insert
    - scripts/backfill.py exists and contains argparse with --tickers and INSERT OR IGNORE and content_hash and CompanyRepo and DocumentRepo and ticker regex validation
    - scripts/backfill.py when invoked as python -m scripts.backfill --tickers AAPL,XOM or via function populates raw_documents and companies and company_aliases
    - tests/integration/test_backfill.py passes and asserts second run adds zero rows via content_hash dedup
    - tests/integration/test_edgar.py passes with respx mocked SEC and asserts Item 1 and Item 1A rows
    - tests/unit/test_yfinance_meta.py passes with mock.patch yfinance.Ticker and asserts external_esg_score only on companies not signals
    - PYTHONPATH=src pytest -q full suite passes with no live network calls (respx plus mock.patch yfinance)
  </acceptance_criteria>
  <done>yfinance runs via to_thread with alias variants and external_esg_score isolation, EDGAR integration via respx proves CIK cache and Item split, and backfill CLI is idempotent via content_hash UNIQUE and satisfies all six Definition of Done checks with no live network in tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SEC HTTP → edgar.py | company_tickers.json, submissions, and filing HTML from sec.gov are external and attacker-influenceable |
| yfinance → yfinance_meta.py | yfinance Ticker.info and sustainability via Yahoo scraper cross trust boundary, may be empty or malformed |
| ticker CLI arg → backfill.py | --tickers values are user-supplied and must be validated before DB or URL use |
| raw_documents insert → SQLite | all collector outputs are persisted with parameterised SQL and UNIQUE dedup |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | src/esg_lens/collectors/edgar.py CIK cache | mitigate | Cache file at data/cache/company_tickers.json with 7d TTL; ticker→CIK mapping built from SEC dict shape handling both dict and list; CIK zero-padded 10 digits and stripped of leading zeros only for path segment; test covers both shapes |
| T-03-02 | Tampering | src/esg_lens/collectors/edgar.py filing URL | mitigate | accessionNumber dashes removed via replace and cik lstrip 0 before interpolating into https://www.sec.gov/Archives/edgar/data/{cik}/{acc_nodash}/{doc} hardcoded host; ticker never appears as host; test asserts 404 handling logs and skips |
| T-03-03 | Tampering | src/esg_lens/collectors/edgar.py HTML parsing | mitigate | BeautifulSoup with lxml/html.parser extracts text then regex ^\s*ITEM\s+1A? with IGNORECASE MULTILINE avoids ReDoS via anchored pattern and bounded 10-K size; filing fetch timeout 30s; test uses excerpt with &nbsp; and nested tags to prove robustness |
| T-03-04 | Spoofing | src/esg_lens/collectors/yfinance_meta.py sync call | mitigate | Sync yfinance wrapped via asyncio.to_thread with 15s wait_for timeout so event loop never blocks; exception caught and logged via structlog; graceful None returns no row instead of crash; test patches yfinance.Ticker |
| T-03-05 | Information Disclosure | src/esg_lens/collectors/yfinance_meta.py sustainability | mitigate | Sustainability DataFrame value written only to companies.external_esg_score with provider yfinance; never inserted into esg_signals; grep gate asserts no external_esg_score reference in scoring path; test verifies isolation |
| T-03-06 | Tampering | scripts/backfill.py ticker validation | mitigate | Ticker uppercased and validated against ^[A-Z0-9.\-]{1,10}$ and 25-ticker cap per OBS-02; parameterised SQL via DocumentRepo INSERT OR IGNORE on (ticker,content_hash) prevents injection; re-run zero new rows proves dedup |
| T-03-07 | Information Disclosure | scripts/backfill.py error handling | mitigate | Collectors never raise via safe_fetch; backfill catches per-collector exceptions, logs via structlog JSON, writes collection_runs error truncated to 1000 chars; no secrets in logs |
| T-03-SC | Tampering | pip installs | mitigate | New dependency beautifulsoup4 4.15.0 plus lxml audited as [OK] in RESEARCH.md Package Legitimacy Audit with 15+ year history and 30M downloads; no [SUS] or [SLOP]; installed from PyPI |
</threat_model>

<verification>
- [ ] PYTHONPATH=src pytest tests/unit/test_edgar_split.py -q passes
- [ ] PYTHONPATH=src pytest tests/unit/test_yfinance_meta.py -q passes with mock.patch yfinance.Ticker
- [ ] PYTHONPATH=src pytest tests/integration/test_edgar.py -q passes with respx mocked SEC returning Item 1 and Item 1A rows
- [ ] PYTHONPATH=src pytest tests/integration/test_backfill.py -q passes and proves re-run adds zero rows
- [ ] grep -n "beautifulsoup4" pyproject.toml succeeds
- [ ] grep -n "asyncio.to_thread" src/esg_lens/collectors/yfinance_meta.py succeeds
- [ ] grep -n "INSERT OR IGNORE" src/esg_lens/db/repositories.py or scripts/backfill.py succeeds
- [ ] PYTHONPATH=src pytest -q total suite green with no live network calls
</verification>

<success_criteria>
- EDGAR ticker to CIK via 7d cached dict-keyed company_tickers.json, submissions parallel arrays, dash-stripped accession URLs, and BeautifulSoup Item 1/1A split with one row per section
- yfinance via asyncio.to_thread seeds company_aliases variants and writes sustainability only to external_esg_score
- scripts/backfill.py --tickers populates raw_documents and is idempotent on content_hash
- All six Phase 1 success criteria true and integration tests prove no live network via respx plus mock.patch yfinance
</success_criteria>

<output>
Create .planning/phases/01-collectors/01-03-SUMMARY.md when done
</output>
